using Windows.UI.Xaml.Controls;

namespace RadialControls
{
    public sealed partial class RadialTimePicker : UserControl
    {
        // Background, MinuteForeground, HourForeground
        // Determine hand sizes dynamically (with parts)
        // Expose control template for hand

        public RadialTimePicker()
        {
            InitializeComponent();
        }
    }
}
